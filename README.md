
# Building

This is set up to use gcc. On Windows, you should install [MSYS2](https://www.msys2.org/), and use that to install gcc.

Don't install the dependencies via MSYS2 though, as many of its packages are the incorrect version.

## Dependencies
[SFML 3](https://www.sfml-dev.org/)